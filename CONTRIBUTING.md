# Contribution

- créer une branche par fonctionnalité ;
- ajouter ou adapter les tests ;
- ne jamais committer de clé API ;
- garder les changements réversibles ;
- documenter les migrations SQLite ;
- exécuter `pytest` avant chaque commit.
